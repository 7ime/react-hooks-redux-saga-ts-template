const static = 'static';
const media = static + '/media';
const fonts = static + '/fonts';
const other = static + '/other';
const dist = 'dist';
const source = 'src';

module.exports = {
    static,
    media,
    fonts,
    other,
    dist,
    source
};
